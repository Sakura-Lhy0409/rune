An on-device Apple Foundation Model capable of text generation tasks.
class
 
SystemLanguageModel
SystemLanguageModel
=== SystemLanguageModel
final
 
class
 
SystemLanguageModel
Overview
The 
 refers to the on-device text foundation model that powers Apple
 
Intelligence. Use 
 to access the base version of the model and perform general-purpose
 
text generation tasks. To access a specialized version of the model, initialize the model
 
with 
 to perform tasks like 
. Apple periodically
 
updates 
 in routine OS updates to improve the on-device model’s abilities
 
and performance. Currently, there are 3 model versions that align with:
iOS, iPadOS, macOS, and visionOS 
26.0 - 26.3
iOS, iPadOS, macOS, visionOS 
26.4
iOS, iPadOS, macOS, and visionOS 
27.0
For more information about how model versions affect your app, see
 
.
Before you use the model, you need to verify its availability. Model availability depends on whether
 
the device and region supports Apple Intelligence. For a list of supported devices, see
 
.
Use 
 to change what your app shows to people based on the availability condition:
[CODE] struct GenerativeView: View { |     // Create a reference to the system language model. |     private var model = SystemLanguageModel.default |  |     var body: some View { |         switch model.availability { |         case .available: |             // Show your intelligence UI. |         case .unavailable(.deviceNotEligible): |             // Show an alternative UI. |         case .unavailable(.modelNotReady): |             // The model isn't ready because it's downloading or because |             // of other system reasons. |         case .unavailable(let other): |             // The model is unavailable for an unknown reason. |         } |     } | }
=== Conforms To
=== System language model
=== Getting the default model
=== Creating a model for a use case
=== Accessing the model variant
=== Checking model availability
=== Inspecting model capabilities
=== Counting tokens
=== Handling a language model error
=== Default Implementations
A type that emits notifications to observers when underlying data changes.
protocol
 
Observable
Observable
=== Observable
A type whose values can be implicitly or explicitly copied.
protocol
 
Copyable
Copyable
=== Copyable
A type whose values can persist beyond their immediate local scope.
protocol
 
Escapable
Escapable
=== Escapable
A thread-safe type whose values can be shared across arbitrary concurrent
 
contexts without introducing a risk of data races.
protocol
 
Sendable
Sendable
=== Sendable
A type whose metatype can be shared across arbitrary isolation domains
 
without introducing a risk of data races.
protocol
 
SendableMetatype
SendableMetatype
=== SendableMetatype
=== Technologies
Perform tasks with models that specialize in language understanding, structured output, and tool calling.
Foundation Models
=== Foundation Models
A protocol that you use to interface with a model.
protocol
 
LanguageModel
LanguageModel
=== LanguageModel
A failure that may occur while generating a response when using any language model.
enum
 
LanguageModelError
LanguageModelError
=== LanguageModelError
An on-device Apple Foundation Model capable of text generation tasks.
class
 
SystemLanguageModel
SystemLanguageModel
=== SystemLanguageModel
The availability status for a specific system language model.
enum
 
Availability
Availability
=== SystemLanguageModel.Availability
An error specific to the on-device system language model.
enum
 
Error
Error
=== SystemLanguageModel.Error
A set of controls that flag sensitive content from model input and output.
struct
 
Guardrails
Guardrails
=== SystemLanguageModel.Guardrails
=== LanguageModel Implementations
A type that represents the use case for prompting.
struct
 
UseCase
UseCase
=== SystemLanguageModel.UseCase
A use case for content tagging.
static
 
let
 
contentTagging
: 
SystemLanguageModel
.
UseCase
=== contentTagging
The variant of an on-device model.
struct
 
Variant
Variant
=== SystemLanguageModel.Variant
The availability of the language model.
var
 
availability
: 
SystemLanguageModel
.
Availability
=== availability
The maximum context size in tokens that the model supports.
var
 
contextSize
: 
Int
=== contextSize
The base version of the model.
static
 
var
 
`default`
: 
SystemLanguageModel
=== default
Creates a system language model instance for a specific use case.
convenience
 
init
(
useCase
: 
SystemLanguageModel
.
UseCase
, 
guardrails
: 
SystemLanguageModel
.
Guardrails
)
=== init(useCase:guardrails:)
A Boolean value that indicates whether the system is entirely ready.
var
 
isAvailable
: 
Bool
=== isAvailable
Languages that the model supports.
var
 
supportedLanguages
: 
Set
<
Locale
.
Language
>
=== supportedLanguages
Returns a Boolean value that indicates whether the given locale is supported by the model.
func
 
supportsLocale
(
Locale
) -> 
Bool
=== supportsLocale(_:)
Returns the token count for the specified instructions.
func
 
tokenCount
(
for
:)
=== tokenCount(for:)
The variant of the on-device model backing this instance.
var
 
variant
: 
SystemLanguageModel
.
Variant
=== variant
Access a larger context window and stronger reasoning by routing session requests
 
through Private Cloud Compute.
=== Adding server-side intelligence with Private Cloud Compute
Identify topics, actions, objects, and emotions in input text with a content tagging model.
=== Categorizing and organizing data with content tags
Enhance the experience in your app by prompting an on-device large language model.
=== Generating content and performing tasks with Foundation Models
Create generative experiences that appropriately handle sensitive inputs and respect people.
=== Improving the safety of generative model output
Generate content in the language people prefer when they interact with your app.
=== Supporting languages and locales with Foundation Models
Manage the prompts your app uses by versioning them to make the most out of
 
model improvements.
=== Updating prompts for new model versions
=== Apple Intelligence
Apple Intelligence
